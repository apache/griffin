{
  "name": "prof_batch_test",

  "process.type": "batch",

  "data.sources": [
    {
      "name": "source",
      "connectors": [
        {
          "type": "avro",
          "version": "1.7",
          "config": {
            "file.name": "src/test/resources/users_info_src.avro"
          }
        }
      ]
    }
  ],

  "evaluateRule": {
    "rules": [
      {
        "dsl.type": "griffin-dsl",
        "dq.type": "profiling",
        "rule": "count(source.user_id), source.first_name group by source.first_name order by count(source.user_id) desc, source.first_name asc limit 3",
        "details": {
          "source": "source",
          "profiling": {
            "name": "prof",
            "persist.type": "metric"
          },
          "group.metric": true
        }
      }
    ]
  }
}